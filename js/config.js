var config = {
    tv: 'samsung', //has to match the parameter in the url from switchBoard,
    sensibilityInVolumeMode: 40, // the higher the number the lower the sensibility
    debug: true    // debug mode - will write logs to console if true
};
